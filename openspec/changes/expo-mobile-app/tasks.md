# Tasks: Expo Mobile App (`expo-mobile-app`)

> **Status:** Tasks (from `design.md` §3 file plan + §5 slice ordering; `spec.md` A–N)
> **Session:** artifact store = openspec; strict TDD (`npm test`, vitest); delivery = ask-on-risk; review budget = 400 changed lines/PR

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~8,300–9,500 total diff; ~4,200–4,700 hand-written (remaining = generated: ~1.5–2k `package-lock.json` regen + ~2.5–3k Expo scaffold) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 → PR 2a–2e → PR 3a–3c → PR 4a–4e → PR 5 (15 PRs, see table below) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High
```

**Why High:** 3 of 5 slices cross 400 non-generated lines on their own (slice 1 ≈680; slice 2 ≈1,400; slice 4 ≈1,550); slice 2 also carries the only web *behavior* change (async hydration) and slice 1 the workspace-restructure blast radius (design R1). Recommendation: chained PRs, `stacked-to-main` once the user confirms chain strategy (web stays deployable at every merge; prohibits `feature-branch-chain` isolation for PR 1 because slice 1 is atomic — core files + alias + web imports must land together or the web build dangles). PR 1 and PR 2b/4a are size exceptions (rename-dominant / borderline); everything else fits the 400-line budget.

**Scope legend:** `web-impact` = must keep web deployable + green; `shared-core` = pure TS in `packages/core`, no DOM; `mobile-only` = additive, never blocks web.

**PR slicing proposal (orchestrator applies; each PR ends green `npm test` + deployable web):**

| PR | Unit | Tasks | Est. non-generated lines | vs 400 |
|----|------|-------|--------------------------|--------|
| 1 | Slice 1 whole (mechanical restructure, rename-dominant) | S1.1–S1.7 | ~680 + lockfile (generated) | overflow → `size:exception` (rename-aware review) |
| 2a | Storage contract + async migration | S2.1–S2.4 | ~310 | OK |
| 2b | Hydration + legacy parsers | S2.5, S2.6 | ~400 | borderline → `exception-ok` |
| 2c | Core store factory + async actions (web untouched, transient duplicate) | S2.7, S2.8 | ~350 | OK |
| 2d | Core API service adapter injection + web callers pass adapter | S2.9 | ~220 | OK |
| 2e | Web switchover + barrel + coach-async (removes transient duplicate) | S2.10–S2.13 | ~300 | OK |
| 3a | Expo scaffold + metro (generated-heavy) | S3.1, S3.2 | ~100 + scaffold (generated) | generated → `size:exception` |
| 3b | Mobile hydration-gate shell | S3.3 | ~250 | OK |
| 3c | Mobile adapters + unit tests | S3.4 | ~180 | OK |
| 4a | Card view + board base | S4.1, S4.2a | ~420 | borderline → `exception-ok` |
| 4b | Board interactions | S4.2b | ~330 | OK |
| 4c | Deck browser + custom CRUD | S4.3 | ~350 | OK |
| 4d | Export / import surface | S4.4 | ~200 | OK |
| 4e | Scenario editor | S4.5 | ~180 | OK |
| 5 | Verification sweep (evidence-led) | S5.1–S5.3 | ~100 + manual | OK |

---

## Slice 1 — Monorepo scaffold + mechanical move (zero behavior change)

**Slice gate (design §5):** `npm install` clean off `package-lock.json`; `npm test` green (regression net); `npm run build` + wrangler deploy smoke; `npm run coach` / `update-cards` execute under node.
**TDD mode:** *regression-only* — no new behavior, no RED phase; existing suites are the net. **Slice est. ~680** (+lockfile generated).

- [x] **S1.1 — Restructure root as npm workspaces root and switch scripts to npm.** Rewrite `package.json` scripts: `dev`/`build`/`deploy`/`preview:worker`/`dev:remote` scoped to `--workspace @pokemon-simulator/web`; `coach`/`coach:remote`/`update-cards` → `node scripts/*.ts`; drop bun invocations. Add `"workspaces": ["packages/*", "apps/*"]`. Delete `bun.lock`; regenerate and commit `package-lock.json`. Files: `package.json`, `bun.lock` (del), `package-lock.json` (gen). Trace: A-1, A-2 (§2a). Scope: web-impact. TDD: regression (`npm install` reproducible; `npm run test/build` resolve). Est: ~80 + lockfile. Deps: none (slice 1 head). <!-- sdd-owner: implementation -->
- [x] **S1.2 — Add shared strict `tsconfig.base.json` and fold web tsconfigs.** Create `tsconfig.base.json` (design §2e flags: `strict`, `verbatimModuleSyntax`, `noUnusedLocals/Parameters`, `erasableSyntaxOnly`, `noEmit`, `moduleResolution: bundler`, `paths` → `packages/core/src/*`); root `tsconfig.json` becomes a thin solution-style file; delete `tsconfig.app.json`/`tsconfig.node.json`. Files: `tsconfig.base.json` (new), `tsconfig.json` (mod), `tsconfig.app.json`/`tsconfig.node.json` (del). Trace: A-3 (§2e). Scope: web-impact. TDD: regression (`tsc -b` type-checks under shared flags; no workspace disables strict flags). Est: ~120. Deps: S1.1. <!-- sdd-owner: implementation -->
- [x] **S1.3 — Create core package skeleton and web app package; move web shell files.** `packages/core/`: `package.json` (private, `type: module`, subpath `exports` per design §2e), `tsconfig.json`, `vitest.config.ts` (`environment: node`, `include: ['src/**/*.test.ts']`). `apps/web/`: `package.json` (`@pokemon-simulator/web`), `tsconfig.json` (extends base + DOM libs + `vite/client`), `vitest.config.ts` (keeps web slot, no tests yet), `vite.config.ts` (add `resolve.alias` `@pokemon-simulator/core` → `packages/core/src`; keep `@cloudflare/vite-plugin` + empty `test` block). Git-move web shell files: root `index.html`, `public/*`, `wrangler.jsonc`, `src/index.css`, `src/App.css`, `src/vite-env.d.ts`, `src/assets/*` → `apps/web/…`. Create root `vitest.workspace.ts` aggregating `['packages/core', 'apps/web', 'apps/mobile']`. Files: listed paths. Trace: A-1, A-4, A-5, B-1 (§2e/§2f/§3.1). Scope: web-impact. TDD: regression — `npm test` runs via workspace aggregation; `npm run build` produces deployable dist. Est: ~200 (configs) + renames. Deps: S1.1, S1.2. <!-- sdd-owner: implementation -->
- [x] **S1.4 — Git-move 7 pure sources + 6 test suites into `packages/core/src` with zero content change.** Moves: `types/index.ts`; `data/decks.ts`; `data/cards.generated.ts` (~100KB, B-4); `services/stateExporter.ts`; `services/stateImporter.ts`; `services/promptGenerator.ts`; `services/pokemonTcgApi.ts` (still `localStorage`-stubbed — cache fns are function-local, safe per design §2b note); tests `data/decks.test.ts`, `data/decks.async.test.ts`, `services/stateExporter.test.ts`, `services/stateImporter.test.ts`, `services/promptGenerator.test.ts`, `services/pokemonTcgApi.test.ts`. No edits (relative imports within core stay valid). Files: the 13 paths (design §3.2). Trace: B-1, B-4, H-1 (§3.2). Scope: shared-core. TDD: regression — the moved suites pass unchanged post-move (`npm test`). Est: ~0–40 (rename-detected). Deps: S1.3. <!-- sdd-owner: implementation -->
- [x] **S1.5 — Move web app sources to `apps/web/src` and rewrite imports to `@pokemon-simulator/core`.** Git-move `main.tsx`, `App.tsx`, `components/{BattleField,PokemonCard,ScenarioEditor,ExportPanel}.tsx`, `store/gameStore.ts` + `store/gameStore.test.ts`, then update import specifiers: types/decks/services via `@pokemon-simulator/core` / subpaths (`/data/decks`, `/services/stateExporter`, `/services/stateImporter`, `/services/promptGenerator`, `/services/pokemonTcgApi`). **`gameStore.ts` stays a local `apps/web/src/store/` module this slice** (moves to core in S2.7; design §2d: only file with module-load storage). Files: design §3.2/§3.3 web rows. Trace: A-5, B-2 (partial), H-1 (§3.3). Scope: web-impact. TDD: regression — full `npm test` (gameStore suite still `localStorage`-stubbed, passes from new location) + `npm run build`. Est: ~180 (import rewrites). Deps: S1.4. <!-- sdd-owner: implementation -->
- [x] **S1.6 — Make both CLI scripts runnable under Node type-stripping.** `scripts/coach-server.ts`: shebang `#!/usr/bin/env bun` → `#!/usr/bin/env node`; `import.meta.dir` → `import.meta.dirname`. `scripts/fetch-cards.ts`: shebang → node; import → `packages/core/src/services/pokemonTcgApi.ts`; output → `packages/core/src/data/cards.generated.ts`; update header comment. Files: `scripts/coach-server.ts`, `scripts/fetch-cards.ts`. Trace: A-2 (R4, §2a). Scope: web-impact. TDD: regression — `node scripts/fetch-cards.ts` regenerates the cards file; `npm run coach -- --help`-style smoke runs under node (fallback: add `tsx` dev dep, documented not silent). Est: ~40. Deps: S1.4. <!-- sdd-owner: implementation -->
- [x] **S1.7 — Update lint and ignore configs for workspaces.** `eslint.config.js`: extend `files`/ignores to `packages/*`, `apps/*`; `globals.browser` only for web workspace, node globals for scripts/core. `.gitignore`: add `apps/mobile/node_modules`, `.expo`, native build dirs. Files: `eslint.config.js`, `.gitignore`. Trace: A-1, A-3 (§3.3). Scope: web-impact. TDD: regression — `npm run lint` passes across workspaces. Est: ~40. Deps: S1.3. <!-- sdd-owner: implementation -->

---

## Slice 2 — Storage adapter + async hydration (behavior change; strict TDD RED→GREEN)

**Slice gate (design §5):** zero `localStorage`/`AsyncStorage` literal in `packages/core` except adapter impls (SC7); hydration before render on web; legacy/malformed tolerance; scenario survives web reload (R5); rapid autosave last-write-wins. **Slice est. ~1,400.** PR order inside slice: 2a → 2b → 2c → 2d → 2e (each green).

- [x] **S2.1 — RED: write in-memory storage conformance + key-contract tests.** New `packages/core/src/storage/types.test.ts`: get/set/remove round-trip, `null` on missing key, `removeItem` idempotency, `dump()` introspection, `STORAGE_KEYS` has exactly the 7 keys (C-1, SC7), key set frozen. Files: `packages/core/src/storage/types.test.ts` (new). Trace: C-1, H-1, H-2 (§2f, §4.1). Scope: shared-core. TDD: red (module missing → compile/test failure). Est: ~60. Deps: S1.4. <!-- sdd-owner: implementation -->
- [x] **S2.2 — GREEN: implement the storage contract.** `packages/core/src/storage/types.ts`: `StorageAdapter` interface (`getItem/setItem/removeItem`, all `Promise`), `STORAGE_KEYS` (7 keys per design §4.1), `StorageKey` type, `createInMemoryStorage(seed?)` with `dump()`. Files: `packages/core/src/storage/types.ts` (new). Trace: C-1 (§4.1). Scope: shared-core. TDD: green. Est: ~70. Deps: S2.1. <!-- sdd-owner: implementation -->
- [x] **S2.3 — RED: write migration tests.** New `packages/core/src/store/migrate.test.ts`: version key absent → set current `DATA_VERSION`; stale version → upgrade without data loss; malformed `pokemon-custom-decks`/`pokemon-scenarios` → never throws, stale keys removed; idempotent across two runs. Files: `packages/core/src/store/migrate.test.ts` (new). Trace: C-4, H-2 (§2f). Scope: shared-core. TDD: red. Est: ~110. Deps: S2.2. <!-- sdd-owner: implementation -->
- [x] **S2.4 — GREEN: implement async `migrateData(adapter, dataVersion?)`.** Lives in `packages/core/src/store/hydrate.ts` (or colocated in `gameStore.ts` per design §2d). Never throws; reads `pokemon-data-version` via adapter; removes stale keys; writes current version. Files: `packages/core/src/store/hydrate.ts` (new, partial). Trace: C-4, §4.2. Scope: shared-core. TDD: green. Est: ~70. Deps: S2.3. <!-- sdd-owner: implementation -->
- [x] **S2.5 — RED: write hydration-order/legacy-tolerance tests.** New `packages/core/src/store/hydrate.test.ts`: hydration order (migrate → autosave → customDecks → scenarios); autosave restore seeds `gameState`/`player1Deck`/`player2Deck`; malformed autosave boots clean (never throws, C-4); **scenario read-back** seeds `scenarios` (R5, F-3); empty storage seeds clean default (C-6); **idempotent** across double-call (D-2); `resetGame` after hydrate restores hydrated decks not null-on-fresh (R6). Files: `packages/core/src/store/hydrate.test.ts` (new). Trace: C-3, C-4, C-6, F-3, H-2 (§1.1, §4.2). Scope: shared-core. TDD: red. Est: ~220. Deps: S2.4. <!-- sdd-owner: implementation -->
- [x] **S2.6 — GREEN: implement `hydrate()` + legacy parsers.** `parseAutoSave(raw)` = verbatim move of `loadAutoSave`/`isValidPlayerState` logic (design §2d: **no behavioral change to the parser**); `parseDecks`/`parseScenarios` tolerant parsers; `hydrate(store, adapter)` runs `migrateData` → reads `pokemon-autosave`/`pokemon-custom-decks`/`pokemon-scenarios` → seeds `gameState`, `player1Deck`, `player2Deck`, `customDecks`, `scenarios` → records hydrated decks in factory closure for `resetGame`. Files: `packages/core/src/store/hydrate.ts` (+ parsers). Trace: C-3, C-4, C-6, F-3 (§1.1, §4.2). Scope: shared-core. TDD: green. Est: ~180. Deps: S2.5. <!-- sdd-owner: implementation -->
- [x] **S2.7 — REFACTOR: move + refactor `gameStore.ts` into core as factory with async persistence.** Move `apps/web/src/store/gameStore.ts` → `packages/core/src/store/gameStore.ts`; replace bare `create()` with `createGameStore(adapter)` (sync init via `createInitialGameState()`, registers fire-and-forget async autosave `subscribe` capturing adapter); delete module-level `loadAutoSave()` + module-level `subscribe` (C-3: no storage at import); `resetGame` restores the hydrated decks recorded by `hydrate()` (R6); manual actions `addCustomDeck`/`removeCustomDeck`/`loadCustomDecks`/`saveScenario`/`deleteScenario` become `async` and `await` adapter writes (C-5); keep `hasActiveGame`. **Web keeps its local copy until S2.11 (transient duplication, removed there — B-2 checks end state).** Files: `packages/core/src/store/gameStore.ts` (moved+modified), `apps/web/src/store/gameStore.ts` (this PR: unchanged copy). Trace: B-2, C-3, C-5, R2/R6 (§2d, §4.2). Scope: shared-core. TDD: red→green via adapted suite (next task). Est: ~200. Deps: S2.6. <!-- sdd-owner: implementation -->
- [x] **S2.8 — REFACTOR: adapt `gameStore.test.ts` into core with in-memory adapter + `await hydrate()`.** Move `apps/web/src/store/gameStore.test.ts` → `packages/core/src/store/gameStore.test.ts`; replace `vi.stubGlobal('localStorage', …)` with `createInMemoryStorage()`; each suite creates `createGameStore(adapter)` and `await hydrate(...)` (empty seed unless primed); **preserve every existing assertion** — including "null on fresh boot" for `resetGame` (R6) and autosave-restore semantics. Files: `packages/core/src/store/gameStore.test.ts` (moved+adapted). Trace: B-2, H-1, R6 (§2f). Scope: shared-core. TDD: adapted suite green under core (regression net re-anchored). Est: ~150. Deps: S2.7. <!-- sdd-owner: implementation -->
- [x] **S2.9 — RED→GREEN: adapter-inject the TCG API service (core + web callers).** In `packages/core/src/services/pokemonTcgApi.ts`: cache fns become async over `adapter` (`getCache(adapter)`, `setCache(adapter, key, cards)`, same for tcgdex); add `adapter: StorageAdapter` as first parameter of `fetchCard`, `fetchDeckCards`, `fetchCardFromTcgdex`, and `parseDeckListWithApi` (design §7 decision); keep fetch/backoff (1.5s/3s/6s)/pacing (100ms)/24h-TTL/8s timeout logic intact (E-4, G-1). Adapt `pokemonTcgApi.test.ts`: `localStorage` stub → in-memory adapter; new cache-round-trip/expiry tests written red first. Update web callers (`apps/web/src/components/BattleField.tsx`, any others) to pass `webStorage` (available post-S2.10; order callers after S2.10 or inline the adapter in this PR — prefer inline ordering: S2.10 first). Files: `packages/core/src/services/pokemonTcgApi.ts`, `packages/core/src/services/pokemonTcgApi.test.ts`, `packages/core/src/data/decks.ts` (signature), web callers. Trace: B-2, C-1, E-4, G-1, H-2 (§4.3, §7). Scope: shared-core (+ web-impact callers). TDD: red→green. Est: ~220. Deps: S2.2 (adapter), S2.8, S2.10 (web callers pass `webStorage`). <!-- sdd-owner: implementation -->
- [x] **S2.10 — GREEN: web `localStorage` adapter + conformance test.** New `apps/web/src/lib/storage.ts`: `webStorage` implementing `StorageAdapter` over `window.localStorage` with exported `getItem/setItem/removeItem` (async-safe — no sync storage at import, C-2). New `apps/web/src/lib/storage.test.ts`: drive identical get/set/remove sequence against a `localStorage` mock (H-1). **Note:** H-1's "all living in the core workspace" is here interpreted as next-to-the-implementation in `apps/web` per design §2f's web vitest slot (conformance measured against the same core contract). Files: `apps/web/src/lib/storage.ts` (new), `apps/web/src/lib/storage.test.ts` (new). Trace: C-2, H-1 (§4.5). Scope: web-impact. TDD: test-first against contract. Est: ~50. Deps: S2.2. <!-- sdd-owner: implementation -->
- [x] **S2.11 — REFACTOR: web async bootstrap switchover (web behavior change).** `apps/web/src/main.tsx`: remove data-version block (now `migrateData`); async bootstrap `createGameStore(webStorage)` → `await hydrate(store, webStorage)` → render; lightweight skeleton/loading gate until hydrated; keep Vite HMR + `beforeunload` guards (web-only). **Delete `apps/web/src/store/`** (`gameStore.ts` + test) — web now imports `createGameStore`/`hydrate`/`hasActiveGame` from `@pokemon-simulator/core` (B-2: no duplicate left). Update `App.tsx`/components that referenced the local store path. Files: `apps/web/src/main.tsx`, `apps/web/src/App.tsx`, `apps/web/src/store/*` (del), `packages/core/src/index.ts` (barrel, grows this PR). Trace: A-5, B-2, C-2, C-3, R2 (§1.1, §3.3, §4.5). Scope: web-impact. TDD: regression — web suite green; manual: reload restores game, scenario survives reload, first-run clean. Est: ~140. Deps: S2.8, S2.10. <!-- sdd-owner: implementation -->
- [ ] **S2.12 — REFACTOR: ExportPanel coach session through the web adapter (7th key).** `apps/web/src/components/ExportPanel.tsx`: `loadCoachSession`/`saveCoachSession`/`clearCoachSession` become async over `webStorage` (design §2d — SC7 requires `pokemon-coach-session` to flow through the adapter; coach stays web-only, D-3). Files: `apps/web/src/components/ExportPanel.tsx`. Trace: C-1, D-3, SC7 (§2d). Scope: web-impact. TDD: regression — grep web for direct `localStorage` now finds only `apps/web/src/lib/storage.ts`. Est: ~60. Deps: S2.11. <!-- sdd-owner: implementation -->
- [ ] **S2.13 — GREEN: core barrel + DOM-free boundary scan.** `packages/core/src/index.ts`: re-export types, storage contract, store factory/hydrate/migrateData/hasActiveGame, stateExporter state fns, stateImporter, promptGenerator; **do not** re-export `pokemonTcgApi`/`decks` from the root (keeps both `CardData` names namespaced under subpaths — R7). Static scan (grep `localStorage|AsyncStorage|window|document|react-dom|react-markdown` in `packages/core/src`, excluding `storage/types.ts`' in-memory impl) must come up clean (B-3, SC7). Files: `packages/core/src/index.ts` (new). Trace: B-3, R7, SC7 (§2e, §4.4). Scope: shared-core. TDD: regression — full `npm test` + scan clean. Est: ~50. Deps: S2.11. <!-- sdd-owner: implementation -->

---

## Slice 3 — Mobile shell (additive; web untouched)

**Slice gate (design §5):** app boots in iOS Simulator skeleton→board; four tabs reachable; background/resume no double-init; no coach entry point; AsyncStorage adapter conforms. **Slice est. ~530 custom** (+scaffold generated).

- [x] **S3.1 — Scaffold the Expo app (generated + minimal custom).** `create-expo-app` SDK 53 template with expo-router into `apps/mobile`; commit generated `package.json` (`@pokemon-simulator/mobile`), `app.json`, `babel.config.js`, `tsconfig.json` (`expo/tsconfig.base`, extends root `tsconfig.base.json`), default `app/` + `components/` template files. Add root script `"build:mobile"`. Files: `apps/mobile/**` (generated), root `package.json`. Trace: D-1 (§2c, §5.4). Scope: mobile-only. TDD: n/a — sanity `npx expo export`/`expo-doctor` clean. Est: ~60 custom + ~2.5–3k generated. Deps: S1.1. <!-- sdd-owner: implementation -->
- [x] **S3.2 — Metro monorepo resolution (R3).** `apps/mobile/metro.config.js`: Expo default + `watchFolders = [workspaceRoot]`, `resolver.nodeModulesPaths = [root node_modules, app node_modules]`, `unstable_enableSymlinks` + `unstable_enablePackageExports` true (design §2b). Verify a `@pokemon-simulator/core` source import bundles through Metro (babel-preset-expo transpiles `.ts`). Fallback if flaky: `babel-plugin-module-resolver` alias → `packages/core/src`, drop package-exports flag (documented, not silent). Files: `apps/mobile/metro.config.js` (new), smoke file in `app/_layout.tsx`. Trace: A-1, B-1, R3 (§2b). Scope: mobile-only. TDD: n/a — Metro bundle + simulator boot. Est: ~40 (+30 fallback). Deps: S3.1, S2.2. <!-- sdd-owner: implementation -->
- [x] **S3.3 — Mobile hydration-gate shell (simulator-evidence).** `app/_layout.tsx`: skeleton gate → `hydrate` once → tabs; `components/StorageProvider.tsx` (context: adapter + hydrated flag); `hooks/useStorage.ts` (`useHydrated()`, idempotent init — resumed app must NOT re-hydrate or reset, D-2); four `app/(tabs)/{index,decks,export,scenarios}.tsx` stubs wired to `<Tabs>`; game board is default landing tab (D-1); **zero coach entry point** (D-3); browser-API replacements deferred to slice 4 (D-4). Files: `apps/mobile/app/_layout.tsx`, `apps/mobile/app/(tabs)/*.tsx`, `apps/mobile/components/StorageProvider.tsx`, `apps/mobile/hooks/useStorage.ts`. Trace: D-1, D-2, D-3, C-3 (§1.1, §2c). Scope: mobile-only. TDD: simulator evidence only (H-3: no jest-expo) — boots skeleton→board; background/resume keeps in-flight game. Est: ~250. Deps: S3.2, S2.6. <!-- sdd-owner: implementation -->
- [x] **S3.4 — RED→GREEN: mobile adapters + clipboard wrapper + unit tests.** RED: `apps/mobile/lib/storage.test.ts` (get/set/remove conformance vs core contract, `vi.mock('@react-native-async-storage/async-storage')`) and `apps/mobile/lib/clipboard.test.ts` (`vi.mock('expo-clipboard'/'expo-sharing')`: `copyText` writes clipboard; `shareText` writes temp file + opens share sheet). GREEN: `apps/mobile/lib/storage.ts` (`mobileStorage` over AsyncStorage, design §4.5) and `apps/mobile/lib/clipboard.ts` (`copyText`, `shareText`). Add `apps/mobile/vitest.config.ts` (`environment: node`, `include: ['lib/**/*.test.ts']`). Files: listed under `apps/mobile/lib/`, `apps/mobile/vitest.config.ts`. Trace: C-2, D-4, F-1, H-3 (§2f, §4.5). Scope: mobile-only. TDD: red→green — root `npm test` now includes mobile workspace. Est: ~180. Deps: S3.3, S2.2. <!-- sdd-owner: implementation -->

---

## Slice 4 — Screens (additive; web untouched)

**Slice gate (design §5):** iOS + Android acceptance — kill-and-relaunch exact restore (E-2), deck CRUD persists (E-3), import/export round-trip (F-1/F-2), scenario lifecycle (F-3), offline play (G-2). **Slice est. ~1,550** (mobile-only; simulator evidence per H-3). All screens consume core store/services via `StorageProvider` + `useStorage`.

- [x] **S4.1 — `PokemonCardView` (mobile-first card render).** RN `<Image>` card, HP, energy, status, damage; fails gracefully when card image unresolved (G-1 non-blocking). Files: `apps/mobile/components/PokemonCardView.tsx`. Trace: E-1, E-4 (§2c). Scope: mobile-only. TDD: simulator evidence — renders preset + API-resolved cards. Est: ~120. Deps: S3.3, S3.4. <!-- sdd-owner: implementation -->
- [x] **S4.2a — `GameBoard` base + primary loop.** `apps/mobile/components/GameBoard.tsx` + `app/(tabs)/index.tsx` wiring: active/bench zones, start game, place Pokémon from deck, swap players, reset — state transitions ONLY through core `gameStore` actions (E-1 parity of rules). Confirm via `Alert`/`Modal`/`Pressable`, no document listeners/portals (D-4). Files: `apps/mobile/components/GameBoard.tsx`, `apps/mobile/app/(tabs)/index.tsx`. Trace: E-1, D-4 (§2c). Scope: mobile-only. TDD: simulator evidence — complete loop for start/place/swap/reset. Est: ~300. Deps: S4.1. <!-- sdd-owner: implementation -->
- [x] **S4.2b — `GameBoard` full interaction surface.** Add/remove energy, add damage, set status; hand/discard/prizes/deck views (view-or-draw style); responsive with large decks (E-4); autosave-driven restore surfaces on relaunch (E-2 via core hydrate). Files: `apps/mobile/components/GameBoard.tsx`. Trace: E-1, E-2, E-4 (§2c). Scope: mobile-only. TDD: simulator evidence — damage+energy+status mutate and board reflects core-computed state; kill-and-relaunch restores exact state (recorded evidence). Est: ~330. Deps: S4.2a. <!-- sdd-owner: implementation -->
- [ ] **S4.3 — `DeckSelectorModal` + deck browser tab.** `app/(tabs)/decks.tsx` + `apps/mobile/components/DeckSelectorModal.tsx`: built-in presets from core data (B-4 bundle load), custom deck add/remove async persisted via adapter (C-5, E-3 surviving restart), external deck-list resolution through core `pokemonTcgApi` with 100ms pacing + cache-first (E-4, G-1); input via `Modal`, confirm via `Alert` (D-4). Files: `apps/mobile/components/DeckSelectorModal.tsx`, `apps/mobile/app/(tabs)/decks.tsx`. Trace: E-3, E-4, G-1, D-4 (§2c). Scope: mobile-only. TDD: simulator evidence — CRUD survives restart; second resolution within 24h serves from cache (network toggled off); card data loads without error (B-4). Est: ~350. Deps: S4.2b, S2.9. <!-- sdd-owner: implementation -->
- [ ] **S4.4 — `ExportPanelView` + export tab (import/export parity).** `app/(tabs)/export.tsx` + `apps/mobile/components/ExportPanelView.tsx`: export current state via core `stateExporter` → `lib/clipboard.copyText` + `shareText` (F-1, identical text to web); import from clipboard via core `stateImporter` with round-trip equality (F-2); no `navigator.clipboard`/`document.createElement` (D-4). Files: `apps/mobile/components/ExportPanelView.tsx`, `apps/mobile/app/(tabs)/export.tsx`. Trace: F-1, F-2, D-4 (§2c). Scope: mobile-only. TDD: core round-trip already unit-covered (shared-core); mobile: simulator evidence — clipboard + share payloads byte-identical to web export of same state. Est: ~200. Deps: S4.2b, S3.4. <!-- sdd-owner: implementation -->
- [ ] **S4.5 — `ScenarioEditorView` + scenarios tab.** `app/(tabs)/scenarios.tsx` + `apps/mobile/components/ScenarioEditorView.tsx`: save current game as named scenario, load, delete — async adapter-persisted (C-5), surviving restarts (F-3, read-back seeded by core `hydrate` from S2.6). Files: `apps/mobile/components/ScenarioEditorView.tsx`, `apps/mobile/app/(tabs)/scenarios.tsx`. Trace: F-3, C-5 (§2c). Scope: mobile-only. TDD: simulator evidence — lifecycle across restart (save → relaunch → load → delete → relaunch). Est: ~180. Deps: S4.2b, S3.4. <!-- sdd-owner: implementation -->

---

## Slice 5 — Verification sweep (evidence-led; no new surface)

**Slice gate (design §5):** full acceptance matrix; bundle sanity (B-4/N-1); `npm test` aggregate; deploy smoke. **Slice est. ~100** + manual evidence.

- [ ] **S5.1 — Web parity + deploy smoke.** `npm run build` + wrangler deploy smoke from the monorepo; web plays identically (start game, autosave reload restore, import/export, scenario save); web coach unchanged (D-3); static scan: no `localStorage` outside `apps/web/src/lib/storage.ts`; no duplicate store/API logic in `apps/web` (B-2). Files: evidence report in `openspec/changes/expo-mobile-app/`. Trace: A-5, B-2, C-1, D-3, SC4/SC7. Scope: web-impact. TDD: regression — full `npm test` + build + deploy smoke. Est: ~50. Deps: S2.13. <!-- sdd-owner: implementation -->
- [ ] **S5.2 — Mobile acceptance matrix (iOS Simulator + Android emulator).** Kill-and-relaunch exact restore both platforms (E-2/SC2/SC3); custom-deck CRUD persists (E-3/SC6); export clipboard + share identical (F-1); import round-trip (F-2); scenario lifecycle (F-3); airplane-mode full game — autosave/decks/scenarios work, only external lookups degrade cache-first (G-2); cold-start time + memory with ~100KB cards measured and recorded vs pre-change web baseline (N-1, R8); four surfaces reachable, board default (D-1). Files: evidence report (screenshots/logs/measurements) in the change dir. Trace: D-1, E-2/E-3, F-1/F-2/F-3, G-2, N-1. Scope: mobile-only. TDD: simulator evidence (H-3). Est: ~30 + manual. Deps: S4.1–S4.5. <!-- sdd-owner: implementation -->
- [ ] **S5.3 — Spec traceability report + aggregate gate.** Map all 20 spec requirements (A-1…N-1) to evidence (test names, scan results, simulator checks); root `npm test` exits 0 across core+web+mobile; record any residual risk (R1–R11 disposition). Files: `openspec/changes/expo-mobile-app/verify-input.md`-style report (name TBD by sdd-verify). Trace: H-1/H-2/H-3, A-4 (§5 slice 5). Scope: mixed. TDD: aggregate green. Est: ~20. Deps: S5.1, S5.2. <!-- sdd-owner: implementation -->

---

## Lifecycle gates (parent-owned, run after implementation work)

- [ ] Start or reuse bounded review per PR before merge — lens by risk: PR 1/2c/2e → `review-risk` (workspace restructure, storage boundary); PR 2d/4c → `review-resilience` (API backoff/cache, offline); all behavior slices → `review-reliability`; PR 4a/4b/4c (large) → full 4R per Review Lens Selection. Verify each ledger finding per the Review Execution Contract. <!-- sdd-owner: parent -->
- [ ] Apply the Review Workload Guard: confirm this forecast (High risk, chained recommended) with the user under `ask-on-risk`; resolve `chain_strategy` (`stacked-to-main` vs `feature-branch-chain`) before PR 1; approve the listed `size:exception` PRs (1, 3a) and borderline ones (2b, 4a). <!-- sdd-owner: parent -->
- [ ] Run `sdd-verify` against `spec.md` (20 requirements, acceptance matrix) with this task list + apply-progress as inputs; remediate CRITICAL/WARNING findings before archive. <!-- sdd-owner: parent -->
- [ ] On verify pass, run `sdd-archive` to close the change and persist the final state. <!-- sdd-owner: parent -->